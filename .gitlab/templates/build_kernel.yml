.build_kernel:
  image: "$CI_REGISTRY_IMAGE"
  stage: kernel_build
  variables:
    KERNEL_VER: ""
    NVIDIA_VER: ""
    DTS_VER:    ""
    PATCH:      ""
    CPUS:       4
  script:
    - ./l4t_kernel_prep_rel32.sh ./linux
  artifacts:
    paths:
      - ./linux/Image
      - ./linux/tegra210-icosa.dtb
      - ./linux/modules.tar.gz
      - ./linux/update.tar.gz
